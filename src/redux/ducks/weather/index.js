import reducer from "./reducers";

import * as weatherSelectors from "./selectors";
import * as weatherOperations from "./operations";
import * as weatherTypes from "./types";

export { weatherSelectors, weatherOperations, weatherTypes };

export default reducer;